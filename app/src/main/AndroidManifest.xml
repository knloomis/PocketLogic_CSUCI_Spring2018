<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketlogic.pocketlogic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".selectLevel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".selectLevel2" />
        <activity android:name=".activeLevel" />
        <activity android:name=".evaluation" />
        <activity
            android:name=".help"
            android:theme="@style/AppTheme.HelpPopUp" />
        <activity android:name=".highscores" />
        <activity android:name=".ingameMenu" />
        <activity android:name=".winScreen" />
        <activity
            android:name=".RestartConfirmPopupActivity"
            android:theme="@style/AppTheme.PopUp" />
        <activity
            android:name=".QuitConfirmPopupActivity"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".gameplay" />
        <activity android:name=".GameScene" />
        <activity android:name=".TutorialTest"></activity>
    </application>

</manifest>